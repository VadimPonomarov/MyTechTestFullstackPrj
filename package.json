{
  "name": "technical_task_fullstack",
  "version": "1.0.0",
  "dependencies": {
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "build": "docker-compose build && npm i --prefix frontend",
    "start": "npm run build --prefix frontend && docker-compose up"
  }
}
